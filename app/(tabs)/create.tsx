import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Create = () => {
  return (
    <SafeAreaView className="bg-primary h-full">
      <Text className="text-lg text-white px-5">Create</Text>
    </SafeAreaView>
  );
};

export default Create;
